name: accessible_motion_example
description: Demonstrates the accessible_motion package.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  accessible_motion:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
